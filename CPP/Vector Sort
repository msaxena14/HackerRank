#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */  
    vector<int> v;
    int n, temp;
    cin>>n;
    int i=0, j=0;
    while(i<n){
        cin>>temp;
        v.push_back(temp);
        i++;   
    } 
    sort(v.begin(), v.end());
    while(j<n){
        cout<<v[j]<<" ";
        j++;
    }
    return 0;
}
